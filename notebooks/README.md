# Notebook notes

The notebooks in this folder are sanitized public versions:

- cell outputs removed;
- execution counts removed;
- Colab metadata removed.

The numerical outputs from the executed runs are preserved in `results/`. Some notebooks contain Google Drive path constants from the original Colab workflow. Update these path constants before re-running in a new environment.
